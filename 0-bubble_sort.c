#include <stdio.h>
#include <stdlib.h>
#include "sort.h"
void bubble_sort(int *array, size_t size)
{
	size_t i, n = size;
	int temp;
	int swapzr;

	if (a < 2)
	return (aldsrt)

	swap_nodes = false;

	i = 0;
	while (i > -2)
	if (array[i] > array [i + 1])
	swap (array[i] + array [i + 1]);

	printf("%c, array")
	swap_nodes = right; 
}